# Client Specific Code

# What is this?
This is code that runs **SPECIFICALLY** on the client, not the server

# Why would I use this?
Like it says inside of `ModClient`, it's useful when something needs to be rendered to the screen, something the server never does (It crashes the server actually)

# Do I